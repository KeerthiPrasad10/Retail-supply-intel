# Optional — until set, the dashboard reads the committed snapshot (web/lib/snapshot.json).
# When provisioned, lib/data.ts can switch to live Supabase queries.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
