"""Retail Supply Intel pipelines.

Ingestion connectors (demand + supply signals), trend scoring, and the
demand-to-supply correlation engine that produces sourcing *triggers*.
"""

__version__ = "0.1.0"
